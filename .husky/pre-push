#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running lint on src/ only..."
npx eslint src/ --config configs/linting/eslint.config.js || echo "⚠️  ESLint found issues, but continuing with push..."

echo "✅ Pre-push checks complete"