#!/bin/sh

echo "🔍 Running quick lint check..."
# Run ESLint with cache and timeout to prevent freezing
timeout 30s npx eslint src/ --config configs/linting/eslint.config.js --cache --max-warnings 50 || echo "⚠️  ESLint timeout or issues found, continuing with push..."

echo "✅ Pre-push checks complete"